{
  "name": "TiffinPoint",
  "version": "1.0.0",
  "private": true,
  "description": "TiffinPoint — meal subscription web app for Delhi/NCR",
  "scripts": {
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "cd backend && npm run build && cd ../frontend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}