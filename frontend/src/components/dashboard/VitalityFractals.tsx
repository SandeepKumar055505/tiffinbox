import React from 'react';

interface Props {
  momentum: number;
}

// Decorative background — stripped to nothing; keep export for compatibility
export const VitalityFractals: React.FC<Props> = () => null;
